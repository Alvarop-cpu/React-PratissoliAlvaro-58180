import cart from "../assets/cart2.svg"

const CartWidget = () => {
    return (
        <div>
            <img src={cart} alt="carrito"/>
            0
        </div>
    )
}

export default CartWidget